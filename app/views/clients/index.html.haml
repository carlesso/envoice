%h1 Listing Clients

= link_to "New Client", new_client_path

%ul
  - @clients.each do |i|
    %li
      = link_to i.company, i
      (#{link_to "remove", i, :confirm => "Are you sure to delete this client?", :method => :delete})